# Certificates

This directory stores machine-generated symbolic or numeric certificates that support the finite/discrete theorem spine.

- `transport_decomposition.json`: symbolic decomposition exported by SymPy
- `interval_check.json`: interval-style numeric check for a concrete finite budget
